language: android
android:
 components:
    # - tools
    - build-tools-27.0.3
    - android-27
    # - extra-android-m2repository

jdk: oraclejdk8

before_install:
- yes | sdkmanager "platforms;android-27"
- chmod +x gradlew
- pip install --user codecov    #Install codecov



# Avoids re-uploading the cache after every build
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.android/build-cache

jobs:
  include:
    - script: ./gradlew :app:core:build
    - script: ./gradlew :app:crypto-openpgp:build
    - script: ./gradlew :app:k9mail:build
    - script: ./gradlew :app:storage:build
    - script: ./gradlew :app:testing:build
    - script: ./gradlew :app:ui:build
    - script: ./gradlew :backend:api:build
    - script: ./gradlew :backend:imap:build
    - script: ./gradlew :backend:pop3:build
    - script: ./gradlew :backend:webdav:build
    - script: ./gradlew :mail:common:build
    - script: ./gradlew :mail:protocols:build
    - script: ./gradlew :mail:protocols:build
    - script: ./gradlew :mail:protocols:build
    - script: ./gradlew :mail:protocols:build
    - script: ./gradlew :mail:testing:build
    - script: ./gradlew :plugins:HoloColorPicker:build
    - script: ./gradlew :plugins:openpgp-api-lib:build
    - script: ./gradlew :plugins:openpgp-api-lib:openpgp-api:build